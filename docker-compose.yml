version: '3.4'

services:
  suitecloud-cli:
    image: skumail/suitecloud-cli
    volumes:
      - suitecloud-cli-config:/root
      - .:/pwd
volumes:
  suitecloud-cli-config:
    name: suitecloud-cli-config
